export enum PicksLeagueMemberRoles {
  COMMISSIONER = "Commissioner",
  MEMBER = "Member",
  NONE = "NONE",
}

export const PICKS_LEAGUE_MEMBER_ROLES_VALUES = Object.values(
  PicksLeagueMemberRoles,
);

export const PICKS_LEAGUE_ROLE_MAX_LENGTH = 32;
